/// @description  player_angle(value);
/// @param value
function player_angle(argument0) {
	 // Define the angle of the player.

	    angle          = (argument0);
	    angle_relative = (wrap_angle(angle));
    
    



}
