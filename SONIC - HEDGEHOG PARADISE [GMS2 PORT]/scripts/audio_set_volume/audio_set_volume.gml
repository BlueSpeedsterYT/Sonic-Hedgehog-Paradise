/// @description  audio_set_volume(id, volume);
/// @param id
/// @param  volume
function audio_set_volume(argument0, argument1) {
	 // Sets the volume of a playing instance.
  
	    audio_sound_gain(argument0, argument1, 0)




}
