/// @description  string_change_if(string, if string, then ostring, else string, then string)
/// @param string
/// @param  if string
/// @param  then ostring
/// @param  else string
/// @param  then string
function string_change_if(argument0, argument1, argument2, argument3, argument4) {
	 // Changes a string to a different one depending on value.
 
	    if(argument0 == argument1)
	    { 
	       return argument2; 
	    }
	    else if(argument0 == argument3)
	    {
	       return argument4;
	    }



}
